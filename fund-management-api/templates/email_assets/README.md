# Email Header Assets

Place the Research Funding CP-KKU email header icon as `research-funding-logo.png` in this directory.

The backend will embed the file as an inline image in outgoing notification emails when present. If the file is missing, a fallback gradient badge will be shown instead.
